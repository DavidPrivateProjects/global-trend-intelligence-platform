name: global_trend_intelligence_platform
version: '1.0.0'
config-version: 2

profile: global_trend_intelligence_platform

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: target
clean-targets:
  - target
  - dbt_packages
  - logs

models:
  global_trend_intelligence_platform:
    +materialized: view
    example:
      +materialized: view
